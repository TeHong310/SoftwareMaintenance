<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                              http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>oms</groupId>
    <artifactId>SoftwareMaintenance</artifactId>
    <version>1.0</version>
    <packaging>jar</packaging>

    <properties>
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <!-- Main and test code live in the same non-standard folder
         (Payment_Vehicle/oms). We only declare it ONCE here as the
         normal source directory, then force the compiler plugin to
         compile it during test-compile (so JUnit, a test-scope
         dependency, is on the classpath). We do NOT also declare
         testSourceDirectory pointing at the same folder, because that
         caused Maven/Surefire to discover and run OrderTest twice
         (once correctly under package oms, once with no package). -->
    <build>
        <sourceDirectory>Payment_Vehicle/oms</sourceDirectory>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <executions>
                    <!-- Disable the normal main compile step... -->
                    <execution>
                        <id>default-compile</id>
                        <phase>none</phase>
                    </execution>
                    <!-- ...and instead compile everything (main + test code)
                         together during the test-compile phase, which has
                         access to test-scope dependencies like JUnit. -->
                    <execution>
                        <id>default-testCompile</id>
                        <phase>test-compile</phase>
                        <goals>
                            <goal>testCompile</goal>
                        </goals>
                        <configuration>
                            <compileSourceRoots>
                                <compileSourceRoot>Payment_Vehicle/oms</compileSourceRoot>
                            </compileSourceRoots>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
                <configuration>
                    <!-- Only look for compiled test classes inside the oms
                         package, avoiding any duplicate/no-package copy. -->
                    <includes>
                        <include>**/oms/*Test.java</include>
                    </includes>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.13.2</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
